apiVersion: v1
kind: Pod
metadata:
  name: armory-web-nginx
spec:
  containers:
  - name: armory-web-nginx
    image: nginx
    ports:
    - containerPort: 80
    volumeMounts:
      - mountPath: /etc/nginx/conf.d/
        name: armory-web-nginx-config
